# Dice Game Flask App

This project is a simple dice game built using Python and Flask.

Rules:
- First roll:
  - 2,3,12 = lose
  - 7,11 = win
  - anything else becomes the point
- Later rolls:
  - 7 = lose
  - roll the point = win

How to run:

pip install flask
python app.py
